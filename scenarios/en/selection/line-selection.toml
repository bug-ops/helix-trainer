# Line Selection Commands
# Scenarios for x (select line) and X (extend to line bounds)

[[scenarios]]
id = "select_line_001"
name = "Select current line"
description = "Use 'x' to select the entire current line"

hints = [
    "'x' selects the current line including newline",
    "Selection will be highlighted",
]

[scenarios.setup]
file_content = """fn main() {
    let x = 1;
    let y = 2;
}"""
cursor_position = [1, 6]

[scenarios.target]
file_content = """fn main() {
    let x = 1;
    let y = 2;
}"""
cursor_position = [1, 0]
selection = [1, 0, 2, 0]

[scenarios.solution]
commands = ["x"]
description = "Press 'x' to select the line"

[scenarios.scoring]
optimal_count = 1
max_points = 100
tolerance = 0

[scenarios.metadata]
category = "selection"
difficulty = "beginner"
tags = ["selection", "line"]
commands_taught = ["x"]
estimated_time_seconds = 5

[[scenarios]]
id = "select_line_002"
name = "Select first line"
description = "Select the first line of the document"

hints = [
    "'x' works on any line",
    "Selects from line start to next line start",
]

[scenarios.setup]
file_content = """use std::io;
fn main() {
    println!("Hello");
}"""
cursor_position = [0, 4]

[scenarios.target]
file_content = """use std::io;
fn main() {
    println!("Hello");
}"""
cursor_position = [0, 0]
selection = [0, 0, 1, 0]

[scenarios.solution]
commands = ["x"]
description = "Press 'x' to select the first line"

[scenarios.scoring]
optimal_count = 1
max_points = 100
tolerance = 0

[scenarios.metadata]
category = "selection"
difficulty = "beginner"
tags = ["selection", "line"]
commands_taught = ["x"]
estimated_time_seconds = 5

[[scenarios]]
id = "extend_line_001"
name = "Extend selection to line bounds"
description = "Use 'X' to extend selection to full line bounds"

hints = [
    "'X' extends the selection to include full lines",
    "Useful after partial selections",
]

[scenarios.setup]
file_content = """struct Config {
    name: String,
    value: i32,
}"""
cursor_position = [1, 6]
selection = [1, 6, 1, 10]

[scenarios.target]
file_content = """struct Config {
    name: String,
    value: i32,
}"""
cursor_position = [1, 0]
selection = [1, 0, 2, 0]

[scenarios.solution]
commands = ["X"]
description = "Press 'X' to extend to line bounds"

[scenarios.scoring]
optimal_count = 1
max_points = 100
tolerance = 0

[scenarios.metadata]
category = "selection"
difficulty = "intermediate"
tags = ["selection", "extend", "line"]
commands_taught = ["X"]
estimated_time_seconds = 5
